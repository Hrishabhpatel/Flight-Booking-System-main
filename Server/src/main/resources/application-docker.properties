spring.application.name=server
server.port=8761

# Eureka Server should not act as client
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false

# Optional but recommended
eureka.instance.hostname=server
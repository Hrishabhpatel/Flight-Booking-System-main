services:
  payment-service:
    build: ./Payment-Gateway
    container_name: payment-service
    ports:
      - "8085:8085"
    environment:
      SPRING_PROFILES_ACTIVE: docker
    depends_on:
      - mysql
